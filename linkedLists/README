This folder holds code for linked lists in C. This code is for a
doubly-linked linear linked list. Current functions include NewList, 
NewNode, PushFront, PushRear, PopFront, PopRear, ForwardWalk, and BackwardWalk.
See list.c for implementation and details.

Future Function Implementations:
1) Read a string of values from a file and put them into a linked list
2) Delete a linked list
3) Ability to add a node at any point in the linked list
